# upstream source trees (submodules kept separately if needed)
wireguard-tools/
wireguard-go-aarch64

# build artifacts we don't want to track
*.o
*.a
admin/synology-wg-admin
bin/boringtun
wireguard-go
